import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ingilizceoykum.app',
  appName: 'My English Story',
  webDir: 'dist'
};

export default config;
